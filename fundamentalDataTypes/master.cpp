int main()
{
    /*
    Smallest unit of memory
        bit (1 or 0)
    Than we have
        byte a goup of bits that are operated on as a unit
        modern standard is that byte is a group of 8 bits
    Memory adress
        holds one byte of data
    */
    return 0;
}
